// Lowest priority style/formatting issues only

function calculateSum(a,b){return a+b}  // Missing spaces around params and braces

const greeting="Hello World"  // Missing spaces around =

// Extra blank lines below



function multiply(x, y)
{  // Opening brace on wrong line
return x*y  // Missing spaces around operator
}

const  numbers  =  [ 1 , 2 , 3 ]  // Inconsistent spacing

// Trailing whitespace on next line
const message = "test"   

export {calculateSum,multiply,greeting}  // Missing spaces after commas
